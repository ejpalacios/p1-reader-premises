apiVersion: 1

datasources:
  - name: premises
    type: postgres
    uid: "premises"
    url: timescaledb:5432
    database: "premises"
    user: postgres
    editable: true
    secureJsonData:
      password: "password"
    jsonData:
      sslmode: "disable" # disable/require/verify-ca/verify-full
      timescaledb: true
